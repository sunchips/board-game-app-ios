name: BoardGameApp
options:
  bundleIdPrefix: com.sanchitb.boardgamerecorder
  deploymentTarget:
    iOS: "26.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: ""
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"

configFiles:
  Debug: Config/Secrets.xcconfig
  Release: Config/Secrets.xcconfig

targets:
  BoardGameApp:
    type: application
    platform: iOS
    sources:
      - path: BoardGameApp
    resources:
      - path: BoardGameApp/Assets.xcassets
    info:
      path: BoardGameApp/Info.plist
      properties:
        CFBundleDisplayName: Board Game
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSRequiresIPhoneOS: true
        UILaunchScreen:
          UIColorName: ""
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        ServerBaseURL: $(SERVER_BASE_URL)
        ApiKey: $(API_KEY)
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.sanchitb.boardgamerecorder
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_USER_SCRIPT_SANDBOXING: YES
        GENERATE_INFOPLIST_FILE: NO

  BoardGameAppTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: BoardGameAppTests
    dependencies:
      - target: BoardGameApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.sanchitb.boardgamerecorder.tests
        GENERATE_INFOPLIST_FILE: YES
