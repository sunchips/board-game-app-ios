// Copy this file to Secrets.xcconfig and edit for your environment.
// Secrets.xcconfig is gitignored so your dev credentials don't leak.

// Simulator uses host's localhost. For a real device, point at the Mac's LAN IP.
SERVER_BASE_URL = http:/$()/localhost:8080
API_KEY = dev-key
